default:
  extensions:
    LeanPHP\Behat\CodeCoverage\Extension:
      auth:       ~
      drivers:
        - local
      filter:     ~
      report:
        # report format (html, clover, php, text)
        format:   clover
        options:
          target: build/behat-coverage
